version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    container_name: backend-container

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    container_name: frontend-container
